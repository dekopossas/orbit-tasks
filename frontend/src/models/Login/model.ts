import { Model, loginRequestResponse, ITenannt } from "./types";

const INITUAL_STATE = {
  user_id: "user_id",
  username: "username",
  name: null,
  tenants: [],
};

export default modelFactory<Model>({
  return 
});
